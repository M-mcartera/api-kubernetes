import AbstractTransformer from './AbstractTransformer';

export default class RestTransformer extends AbstractTransformer {
    /**
     *
     * @param {any} model
     * @returns {Object|string}
     * @abstract
     */
    transform(model) {
        /* */
    }
}
